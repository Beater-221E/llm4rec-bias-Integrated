# Single-GPU profile. Compose with: hardware=single
hardware:
  name: single
  cuda_visible_devices: "0"
  # MLLM Retriever/Ranker CLI is single-process; pin one device.
  mllm_cuda_visible_devices: "0"
  env: {}

training:
  distributed: single
  auto_launch_multi_gpu: false
